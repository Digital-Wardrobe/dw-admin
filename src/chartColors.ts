/*
 * Chart series colours.
 *
 * recharts takes literal values, not Tailwind classes, so the palette lives
 * here as well as in tailwind.config.js. One accent stepped by lightness, so a
 * pie or bar reads as parts of one whole rather than as unrelated categories.
 */
export const SERIES = ['#0E6E6E', '#3E9A97', '#7FBAB6', '#C3D6D4'];
